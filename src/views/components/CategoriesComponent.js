import React from "react";


const CategoriesComponent = (props) => { 

  //resolveCategory(location.pathname);

  /* Fix this */
  //We will use a function to filter only videos that belong to the current path. We might need regex to get rid of the /


  return <h1 style={{ color: "white" }}>Hello</h1>;
};

export default CategoriesComponent;
