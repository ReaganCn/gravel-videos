import {
 CLOSE
} from "./types";
//import {defaultState} from "../rootStore"

const defaultState = {
 
};

const watchingReducer = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default watchingReducer;
